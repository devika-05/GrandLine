import { NgZorroImportsModuleTs } from './ng-zorro-imports-module.ts';

describe('NgZorroImportsModuleTs', () => {
  it('should create an instance', () => {
    expect(new NgZorroImportsModuleTs()).toBeTruthy();
  });
});
